#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: 0 Always success
 */

int main(void)
{
	char first[100], second[100], concat[100];

	printf("Print 2 strings\n");
	scanf("%100s", first);
	scanf("%100s", second);
	strcpy(concat, first);
	str
}
