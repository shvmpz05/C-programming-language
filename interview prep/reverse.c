#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *reverse_words (const char *words, size_t length, char *reversed)
{
/*
'words' is nul-terminated, but its 'length' is given for convenience.
Write to 'reversed' and return it.
*/
   size_t word_start = length - 1;

}

int main(void) {
    char myword[]= "Hello World";
    char mygreeting[] = "World Hello";
    reverse_words(&myword, &mygreeting);
}